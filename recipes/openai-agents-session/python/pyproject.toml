[project]
name = "openai-agents-session-py"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "openai-agents >= 0.2",
    "openai-agents-persql >= 0.1",
    "persql >= 0.4",
    "python-dotenv >= 1.0",
]

[tool.uv]
package = false
