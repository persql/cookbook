[project]
name = "agent-memory-py"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.2",
    "persql>=0.1",
    "python-dotenv>=1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
