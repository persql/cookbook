# PerSQL — create an admin token at https://console.persql.com
# Data agents require an admin-role token to create / update / run / delete.
PERSQL_TOKEN=psql_live_s0_...
# namespace/database-slug — the database the agent is welded to.
PERSQL_DATABASE=myteam/metrics
