# Leave these unset to run against in-process SQLite (no account needed).
# Set them to run the same Kysely code against a real PerSQL database.
# PERSQL_TOKEN=psql_live_...
# PERSQL_DATABASE=your-namespace/your-database
